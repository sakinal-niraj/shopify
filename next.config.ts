import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['cdn.dummyjson.com', 'www.fillmurray.com', 'via.placeholder.com'], // Add the domain here
  },
};

export default nextConfig;
